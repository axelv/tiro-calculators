{
  "name": "tiro-calculators-harness",
  "private": true,
  "version": "0.0.0",
  "description": "FHIR Questionnaire harness + Playwright tests for Tiro cardiology / hem-onc / urology calculators.",
  "scripts": {
    "serve": "bunx serve -p 4173 -L .",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "serve": "^14.2.4"
  }
}
